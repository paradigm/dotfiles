" These cannot be set in ftplugins as they'll be triggered to late.

" Fold vim functions and augroups.
let g:vimsyn_folding = 'af'

" Default to LaTeX, not Plain TeX/ConTeXt/etc
let g:tex_flavor='latex'
